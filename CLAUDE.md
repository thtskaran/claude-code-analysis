# Claude Code Internals Analysis

82 reverse-engineered docs covering Claude Code v2.1.88. 512K lines of TypeScript source analyzed.

The `/internals` skill (`.claude/skills/internals/SKILL.md`) lets any Claude Code agent audit user codebases against these production patterns. It fetches `TREE.md` as a live index — keep that file updated when docs are added, moved, or removed.
